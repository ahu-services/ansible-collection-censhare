---
- name: Converge
  hosts: censhare
  remote_user: asudo
  vars:
    censhare_db_host: "psql14"
    censhare_repo_user: "{{ lookup('ansible.builtin.env', 'CENSHARE_REPO_USER') }}"
    censhare_repo_pass: "{{ lookup('ansible.builtin.env', 'CENSHARE_REPO_PASS') }}"
    censhare_mail_enabled: true
  tasks:
    - name: "Include ahu_services.censhare.server"
      ansible.builtin.include_role:
        name: "ahu_services.censhare.server"
